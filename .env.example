# Meta / WhatsApp Cloud API
WA_PHONE_NUMBER_ID=             # sender phone number ID
WA_ACCESS_TOKEN=                # permanent system-user token
WA_APP_SECRET=                  # for X-Hub-Signature-256 verification
WA_VERIFY_TOKEN=                # arbitrary string; must match Meta webhook config
# Read-only Meta Ads Insights projection (optional: the bot still boots without it)
META_ADS_TOKEN=                 # system-user token with ads_read
META_AD_ACCOUNT_ID=             # act_...
META_GRAPH_VERSION=v25.0        # validated against the live account
AD_CAMPAIGN_MAP=                # campaign_id:campaign_key[,campaign_id:campaign_key]
WA_RECEPTION_TEMPLATE=          # approved Utility template for reception AND staff
                                # notifs (café orders + guardian/coach reminders)
                                # when the 24h window is closed (2 body variables:
                                # {{1}} subject, {{2}} flattened detail).
                                # naming convention: awa_<audience>_<objet>,
                                # e.g. awa_notification. Empty = staff/out-of-window
                                # sends fail 131047 (visible in /admin/notifications).
WA_RECEPTION_TEMPLATE_LANG=fr   # language code of that template
WA_WAITLIST_TEMPLATE=           # approved Utility template when a waitlisted spot
                                # frees up outside the 24h window (2 body vars:
                                # {{1}} class name, {{2}} date/time). Empty =
                                # free-text only. Free text is tried first; template
                                # only on Meta error 131047. Set AFTER Meta approval.
WA_WAITLIST_TEMPLATE_LANG=en    # must match the Meta template language code exactly
                                # (en, en_US, fr, …)

# Wix
WIX_API_KEY=
WIX_SITE_ID=

# Wave
WAVE_API_KEY=
WAVE_WEBHOOK_SECRET=            # HMAC verification of Wave webhooks

# Orange Money / Max It (Sonatel) — all three required to enable rails
# (empty = Wave-only). Same portal app as the website is OK.
OM_CLIENT_ID=
OM_CLIENT_SECRET=
OM_MERCHANT_CODE=               # 6-digit merchant code (e.g. 553651)
OM_API_BASE=https://api.orange-sonatel.com
# OM_SANDBOX_CLIENT_ID=
# OM_SANDBOX_CLIENT_SECRET=

# Anthropic
ANTHROPIC_API_KEY=

# Voice-note transcription (optional — without it, voice notes get a polite
# "please type it" reply)
OPENAI_API_KEY=                 # sk-... (platform.openai.com)
TRANSCRIPTION_MODEL=gpt-4o-mini-transcribe

# Email notifications to reception (optional — events are still logged + in the
# daily summary without it). Brevo HTTP API: SMTP is blocked on Railway.
BREVO_API_KEY=                  # xkeysib-... (Brevo → Settings → SMTP & API)
BREVO_WEBHOOK_TOKEN=            # secret du webhook rebonds (/webhooks/brevo?token=...) ; vide = endpoint inactif
EMAIL_FROM=Awa - Revive <support@revive.sn>   # must be a verified Brevo sender
RECEPTION_EMAIL=support@revive.sn

# Web Push for the ops PWAs (/ops/service lock-screen "commande prête" alerts).
# Empty = PWAs still work, but phones get NO background alert (the 🔔 panel then
# only shows install instructions). Generate a keypair once with:
#   npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=               # base64url, also served to the client to subscribe
VAPID_PRIVATE_KEY=              # keep secret
VAPID_SUBJECT=mailto:support@revive.sn   # contact for the push service (mailto: or https:)

# App
DATABASE_URL=postgres://user:pass@localhost:5432/revive
RECEPTION_PHONE=+221784644329   # human-handoff destination
OWNER_PHONE=+221774982711       # owner's WhatsApp: gets a copy of EVERY alert that
                                # needs a human intervention (refunds, manual
                                # activations, handoffs, stuck clients). Template-first
                                # — this number never messages Awa, so its 24h window
                                # is always closed. Empty = no owner copy.
OWNER_ALERT_ENABLED=true        # false = interventions go to reception only
WA_OWNER_ALERT_TEMPLATE=        # dedicated approved Utility template for those alerts
                                # (2 body vars, same shape as WA_RECEPTION_TEMPLATE).
                                # Empty = reuse WA_RECEPTION_TEMPLATE.
WA_OWNER_ALERT_TEMPLATE_LANG=   # empty = reuse WA_RECEPTION_TEMPLATE_LANG
NEW_CHAT_NOTIFY_PHONE=              # optional WhatsApp ping when someone starts a conversation (empty = off)
NEW_CHAT_NOTIFY_GAP_HOURS=6     # silence gap (h) after which a message counts as a new conversation
PACK_DISCOVERY_META_SOURCE_IDS= # Meta Pack Découverte ad IDs, comma-separated
PACK_DISCOVERY_SERVICE_IDS=     # optional Reformer Wix service IDs, comma-separated
PACK_DISCOVERY_STEP1_PLAN_ID=   # hidden 10 000 FCFA / one-session Wix plan ID
PACK_DISCOVERY_CONTINUATION_PLAN_IDS= # hidden 20 000 FCFA / two-session Wix plan ID
# Comma-separated Wix plan IDs Awa may expose and sell. Required in production.
# Hidden/internal/free plans stay absent even when Wix returns them.
AWA_SELLABLE_PLAN_IDS=
KEYS_AUTOMATION_ENABLED=false
INVITEE_PLAN_ID=
INVITEE_BONUS_PLAN_ID=
HABITUEE_PLAN_ID=
HABITUEE_BONUS_PLAN_ID=
RESIDENTE_PLAN_ID=
RESIDENTE_BONUS_PLAN_ID=
INVITATION_PLAN_ID=
# Plans sur mesure (une cliente précise chacun), séparés par des virgules.
# SUR_MESURE_PLAN_ID (singulier, historique) reste accepté et fusionné. Vide = dark.
SUR_MESURE_PLAN_IDS=
# L'Abonnement Aquabike (famille de Clé distincte). Les quatre vides = type dark.
AQUABIKE_ABO_PLAN_ID=
AQUABIKE_BONUS_PLAN_ID=
AQUABIKE_INVITATION_PLAN_ID=
AQUABIKE_SERVICE_IDS=
KEY_REFORMER_SERVICE_IDS=
KEY_BONUS_SERVICE_IDS=
LEGACY_REFORMER_PLAN_IDS=
INVITEE_HISTORY_PLAN_IDS=
# Tarif abonné massage (le serveur décide, jamais le modèle). Vide = inerte.
# MASSAGE_SERVICE_IDS : id(s) du service Massage (Class capacité 1).
# MASSAGE_MEMBER_PLAN_IDS : plans qui donnent droit au tarif réduit (L'Habituée,
#   La Résidente, plans sur-mesure >= 3x/sem). Exclut L'Invitée et les carnets.
# MASSAGE_MEMBER_RATE_XOF : prix membre (retombe sur le catalogue si >= catalogue).
MASSAGE_SERVICE_IDS=
MASSAGE_MEMBER_PLAN_IDS=
MASSAGE_MEMBER_RATE_XOF=25000
INVITATION_SLOT_HOUR=12
INVITATION_SLOT_MINUTE=30
WA_KEY_INVITEE_J5_TEMPLATE=
WA_KEY_INVITEE_J5_TEMPLATE_LANG=fr
WA_KEY_THIRD_SESSION_TEMPLATE=
WA_KEY_THIRD_SESSION_TEMPLATE_LANG=fr
WA_KEY_MEMBER_J5_TEMPLATE=
WA_KEY_MEMBER_J5_TEMPLATE_LANG=fr
WA_KEY_INVITATION_J10_TEMPLATE=
WA_KEY_INVITATION_J10_TEMPLATE_LANG=en
WA_KEY_MEMBER_J5_INVITATION_TEMPLATE=
WA_KEY_MEMBER_J5_INVITATION_TEMPLATE_LANG=en
WA_KEY_FINISHED_TEMPLATE=
WA_KEY_FINISHED_TEMPLATE_LANG=fr
# Aquabike-family lifecycle templates (empty = that reminder stays dark).
WA_AQUABIKE_INVITATION_J10_TEMPLATE=
WA_AQUABIKE_INVITATION_J10_TEMPLATE_LANG=en
WA_AQUABIKE_MEMBER_J5_TEMPLATE=
WA_AQUABIKE_MEMBER_J5_TEMPLATE_LANG=en
WA_AQUABIKE_MEMBER_J5_INVITATION_TEMPLATE=
WA_AQUABIKE_MEMBER_J5_INVITATION_TEMPLATE_LANG=en
WA_AQUABIKE_FINISHED_TEMPLATE=
WA_AQUABIKE_FINISHED_TEMPLATE_LANG=en
WA_LEGACY_KEY_CONVERSION_TEMPLATE=
WA_LEGACY_KEY_CONVERSION_TEMPLATE_LANG=fr
WIX_WEBHOOK_SHARED_SECRET= # min. 32 chars; same value in Wix Secrets Manager
WIX_WEBHOOK_PUBLIC_KEY=    # optional fallback for native Wix RS256 webhooks
PAYMENTS_LEDGER_START_DATE=2026-07-01 # first accounting day included in /admin/paiements
ADMIN_HUMAN_REPLY_ENABLED=false # guarded admin composer; enable after production verification
WA_ADMIN_FOLLOWUP_TEMPLATE=     # optional fixed client template for follow-up outside the 24h window
WA_ADMIN_FOLLOWUP_TEMPLATE_LANG=fr
AWA_AUTO_RESUME_DEFERRED_ENABLED=false # recover interrupted deferred-Awa batches after relay validation
NOTIF_TEST_PHONE=+221774982711  # where /admin/notifications "Envoyer un test" sends (never the real guardian/coach); empty = test disabled
WA_STORY_TEMPLATE=story_quotidienne # approved template with a dynamic IMAGE header; works outside the 24h window
WA_STORY_TEMPLATE_LANG=en
PAYMENT_LINK_TTL_MINUTES=20
BASE_URL=                       # public https URL of this service
STUDIO_ADDRESS=Revive Studio, Dakar
CLAUDE_MODEL=claude-sonnet-5
PORT=3000

# Restricted team accounts for /admin. Unset = fallback revive/revive@5000.
# Comma-separated "user:password" accounts (one per human, for action audit).
ADMIN_USERS=

# Separate owner login: grants the whole admin, including coach payments.
OWNER_ADMIN_USER=owner
OWNER_ADMIN_PASSWORD=
# Deprecated compatibility fallback; remove after migrating the value above.
OWNER_PAYMENTS_PASSWORD=
